{
  "name": "lazy-bones",
  "version": "0.1.1",
  "description": "Declare how to get all your data in one place, then lazily get just the parts you need.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/DullReferenceException/lazy-bones.git"
  },
  "author": "Jacob Page <jacob.h.page@gmail.com>",
  "publishConfig": {
    "registry": "https://registry.npmjs.com/"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.41",
    "@types/node": "^7.0.18",
    "@types/sinon": "^2.2.1",
    "chai": "^3.5.0",
    "chai-as-promised": "^6.0.0",
    "mocha": "^3.3.0",
    "nyc": "^10.3.2",
    "sinon": "^2.1.0",
    "sinon-chai": "^2.9.0",
    "ts-node": "^3.0.4",
    "typescript": "^2.3.1"
  },
  "dependencies": {
    "bluebird": "^3.5.0"
  },
  "scripts": {
    "test": "nyc --reporter=lcov --reporter=text mocha --compilers ts:ts-node/register ./test --recursive",
    "build": "tsc --p ./tsconfig.json",
    "prepublish": "npm test && npm run build"
  }
}
